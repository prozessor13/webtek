#... some settings during development
{
   code_reload => 1,    # reloads all code on a request
   tpl_debug => 0,      # render the template filename as html-comments
   log_time => 0,       # show timer information in error_log
   log_sql => 0,        # show sql statements in error_log
}